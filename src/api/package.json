{
  "name": "api",
  "version": "1.0.0",
  "description": "Express.js project for database connection",
  "main": "server.js",
  "scripts": {
    "comment-vercel-build": "node ./server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}